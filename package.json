{
  "name": "health-skillz",
  "version": "1.0.0",
  "description": "Claude Skill for analyzing health records via SMART on FHIR",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun run --watch src/server.ts",
    "build:web": "cd web && bun run build.ts",
    "build:brands": "bun run scripts/download-brands.ts",
    "build:skill": "bun run scripts/package-skill.ts",
    "setup": "bun run build:brands && bun run build:skill && bun run build:web"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
